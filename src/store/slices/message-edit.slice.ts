import { createSlice } from "@reduxjs/toolkit";

interface State {}

const state: State = {};

export const messageEditSlice = createSlice({
    name: "message-edit",
    initialState: state,
    reducers: {},
});
